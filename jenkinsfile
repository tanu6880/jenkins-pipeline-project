pipeline {
    agent any

    stages {
        stage('Compile') {
            steps {
                // Compile the Java program
                sh 'javac Main.java'
            }
        }
        stage('Run') {
            steps {
                // Run the compiled Java program
                sh 'java Main'
            }
        }
    }
}
